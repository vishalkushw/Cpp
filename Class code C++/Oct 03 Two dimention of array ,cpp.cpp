                //Two dimention of array 
 /*  In a two dimention arry we use two subscript where  first subscript 
     belong two row and second the subscript belong two columb mostaly
    database uses table as two dimention array in a two dimention 
     array we use two nested loop where first loop dedicated for row and
     the second loop is dedicated for columb it is also called matrics
     
     syntax datatype arrayname [rowsize] [columbsize];
 */    
 
